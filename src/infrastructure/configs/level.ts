export interface ILevelData {
    level: number;
    xp_req: number;

    hp_max: number;
    hp_rate: number;

    ap_max: number;
    ap_rate: number;
    
    atk_min: number;
    atk_max: number;

    crit_rate: number;
    crit_mult: number;
}